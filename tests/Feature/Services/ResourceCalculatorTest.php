<?php

namespace Tests\Feature\Services;

use Tests\TestCase;

class ResourceCalculator extends TestCase
{
    /**
     * A basic test example.
     *
     * @return void
     */
    public function testExample()
    {
        $this->assertTrue(true);
        // $seconds = 5;

        // $village = factory(Village::class)->make([
        //     'last_activity' => Carbon::now()->subSeconds($seconds),
        // ]);

        // $foodCalculator = app(FoodCalculator::class)->make();

        // $food = $foodCalculator->calculate($village);

        // $expected = $seconds * config('game.food_per_level') * 1;
    }
}
